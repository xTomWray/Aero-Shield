# API Workspace

`apps/api/examplesim` currently owns the deterministic example snapshot source used by local demos.

The real Aero Shield backend can still land under `apps/api` later, as long as it satisfies the shared provider contracts in `packages/domain` and can replace the provider used by `apps/web`.
